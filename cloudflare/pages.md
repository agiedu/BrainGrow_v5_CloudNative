# Cloudflare Pages

Connect the GitHub repository and set **Root directory** to `frontend`.
Build command: `npm run build`
Build output directory: `dist`
Environment variable: `VITE_API_URL=https://braingrow-v5-cloudnative.onrender.com`

Do not put database passwords or JWT secrets in Cloudflare frontend variables.
